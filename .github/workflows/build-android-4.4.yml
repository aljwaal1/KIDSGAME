name: Build Kids Game Android 4.4

on:
  workflow_dispatch:
  push:
    branches: [android-4.4]
    paths:
      - 'legacy-android-4.4/**'
      - '.github/workflows/build-android-4.4.yml'

permissions:
  contents: write

concurrency:
  group: kidsgame-android-4-4
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: android-4.4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 33
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-33' 'build-tools;33.0.2'

      - name: Set up Gradle 7.6.4
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '7.6.4'

      - name: Build legacy APK
        run: gradle -p legacy-android-4.4 clean assembleRelease --stacktrace --no-daemon

      - name: Copy APK
        run: |
          test -f legacy-android-4.4/app/build/outputs/apk/release/app-release.apk
          mkdir -p apk
          cp legacy-android-4.4/app/build/outputs/apk/release/app-release.apk apk/kidsgame-android-4.4.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: kidsgame-android-4.4
          path: apk/kidsgame-android-4.4.apk

      - name: Commit APK
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add apk/kidsgame-android-4.4.apk
          if ! git diff --cached --quiet; then
            git commit -m 'Build Kids Game Android 4.4 APK'
            git push origin HEAD:android-4.4
          fi
